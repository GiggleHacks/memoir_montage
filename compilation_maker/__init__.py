__version__ = "0.12.0"
APP_NAME = "CompilationMaker"

# When you make a user-facing change to Memoir Montage, bump __version__ above.
# The GUI reads it via api.app_version() so the brand line ("MEMOIR_MONTAGE v…")
# stays in sync without touching the HTML.
